1 - Android validator-app. Checks NFC field, receives data from keys, shares data with server and show result.
2 - Android key-app. Generates key pair, stores user data and transfers data to validator via NFC.
